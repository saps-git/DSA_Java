package com.dsa.linkedlist;

public class RecursiveLLinsert {
    //TODO: PUT THE BELOW CODE IN LLTemplate.java FILE AND MAKE CHANGES ACCORDINGLY AND RUN
    /*public void insertRecr(int data, int pos) {
        if(pos > size) {
            System.out.println("Invalid position");
            return;
        }
        if(pos == 0) {
            insertFirst(data);
            return;
        }
        LL.Node temp = head;
        helper(data, pos, temp);
    }

    public void helper(int data, int pos, LL.Node temp) {
        if(pos == 1) {
            LL.Node node = new LL.Node(data);
            node.next = temp.next;
            temp.next = node;
            return;
        }

        helper(data, pos-1, temp.next);
    }

    public static void main(String[] args) {
        linkedList.insertRecr(34,6);
    }*/
}
