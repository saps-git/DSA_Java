package com.dsa.questions.leetcode.Linkedlist;

//Leetcode-83

public class RemoveDuplicatesLL {
    //TODO: PUT THE BELOW CODE IN LLTemplate.java FILE AND MAKE CHANGES ACCORDINGLY AND RUN, OR STRAIGHT UP RUN IT ON LEETCODE
    /*public Node deleteDuplicates(Node head) {
        LLTemplate.Node curr = head;
        while(curr.next != null) {
            if(curr.data == curr.next.data) {
                curr.next = curr.next.next;
            } else {
                curr = curr.next;
            }
        }

        return head;
    }

    public static void main(String[] args) {
        linkedList.insert(1);
        linkedList.insert(1);
        linkedList.insert(2);
        linkedList.insert(3);
        linkedList.insert(3);

        linkedList.display();
        linkedList.deleteDuplicates(head);
        linkedList.display();
    }*/
}
